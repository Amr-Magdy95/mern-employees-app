const { validationResult, matchedData } = require("express-validator");
// const errors = validationResult(req);
//  if (!errors.isEmpty())  // send errors
// const data = matchedData(req);

exports.createEmployee = (req, res) => {};
